<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="store.mapper.MemberMapper">
	<resultMap type="Member" id="memberMap">
		<id column="memberId" property="memberId" />
		<result column="password" property="password" />
		<result column="nickname" property="nickname" />
		<result column="point" property="point" />		
		<result column="timeLimit" property="timeLimit" />
		<result column="currentProgress" property="currentProgress"/>
		<result column="goalProgress" property="goalProgress" />
	</resultMap>

	<sql id="selectMember">
		SELECT memberId, password, nickname, point, timeLimit, currentProgress, goalProgress
		FROM member
	</sql>

	<select id="selectMemberById" parameterType="string" resultMap="memberMap">
		<include refid="selectMember" />
		WHERE memberId = #{memberId}
	</select>

	<select id="selectMembersOrderByPoint" resultMap="memberMap">
		<include refid="selectMember" />
		ORDER BY point DESC
	</select>

	<insert id="createMember" parameterType="Member">
		INSERT INTO member (memberId, password, nickname, point, timeLimit, currentProgress, goalProgress)
		VALUES (#{memberId}, #{password}, #{nickname}, #{point}, sysdate, #{currentProgress, jdbcType=INTEGER}, #{goalProgress, jdbcType=INTEGER})
	</insert>
	<update id="updateMember" parameterType="Member">
		UPDATE member
		SET password = #{password}, nickname = #{nickname}, point = #{point}, TO_DATE('timeLimit = #{timeLimit}','YYYY-MON-DD'), currentProgress = #{currentProgress, jdbcType=INTEGER}, goalProgress = #{goalProgress, jdbcType=INTEGER}
		WHERE memberId = #{memberId}
	</update>
	<delete id="deleteMember" parameterType="String">
		DELETE FROM member 
		WHERE memberId = #{memberId}
	</delete>

</mapper>
